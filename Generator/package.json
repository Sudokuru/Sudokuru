{
    "name": "sudokupuzzlegenerator",
    "version": "1.3.0",
    "description": "",
    "license": "GPL-3.0",
    "scripts": {
        "generate": "cross-var npx node ./Generate.js $npm_config_filepath $npm_config_start $npm_config_end $npm_config_batchsize",
        "upload": "cross-var npx node ./Upload.js $npm_config_endpoint $npm_config_token $npm_config_throttle"
    },
    "dependencies": {
        "cross-var": "^1.1.0",
        "node": "^18.13.0"
    }
}
