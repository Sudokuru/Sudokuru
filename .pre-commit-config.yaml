default_stages: [pre-commit, pre-push]
repos:
  - repo: local
    hooks:
      - id: bun
        name: bun
        entry: bun test
        always_run: true
        language: system
        verbose: false